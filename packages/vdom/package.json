{
  "name": "@synxjs/vdom",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "sideEffects": false,
  "files": ["dist/**"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "vitest --silent=false --coverage",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@synxjs/types": "workspace:*",
    "@synxjs/instance": "workspace:*",
    "@synxjs/runtime": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsup": "^8.0.0",
    "eslint": "^8.56.0",
    "@testing-library/jest-dom": "^6.0.0",
    "@vitest/coverage-v8": "^1.0.0",
    "jsdom": "^24.0.0",
    "vitest": "^1.0.0"
  }
}